#include <Arduino.h>

// put function declarations here:
int led1 = D0;
int led2 = D1;
int led3 = LED_BUILTIN;

void setup() {
  // put your setup code here, to run once:
  pinMode(led1, OUTPUT);
  pinMode(led2, OUTPUT);
  pinMode(led2, OUTPUT);
}
